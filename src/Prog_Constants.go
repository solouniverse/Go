package main

import (
	"fmt"
)

func main() {
	// const variables aren't shorthand declarable and once defined it is unchangable.
	const version = 1.6

	fmt.Println("const version", version)

	//version foat64 = 1.7

	//fmt.Println("New version", version)
}
